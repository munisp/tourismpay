apiVersion: apps/v1
kind: Deployment
metadata:
  name: microinsurance-service
  namespace: insurance-platform
  labels:
    app: microinsurance-service
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: microinsurance-service
  template:
    metadata:
      labels:
        app: microinsurance-service
        tier: backend
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "microinsurance-service"
        dapr.io/app-port: "8028"
    spec:
      serviceAccountName: insurance-platform
      containers:
      - name: microinsurance-service
        image: registry.yourdomain.com/insurance/microinsurance-service:latest
        ports:
        - containerPort: 8028
          name: http
        - containerPort: 9090
          name: metrics
        envFrom:
        - configMapRef:
            name: platform-config
        - secretRef:
            name: db-credentials
        - secretRef:
            name: telco-credentials
        env:
        - name: SERVICE_PORT
          value: "8028"
        - name: CROP_INSURANCE_ENABLED
          value: "true"
        - name: LIVESTOCK_INSURANCE_ENABLED
          value: "true"
        - name: WEATHER_PARAMETRIC_ENABLED
          value: "true"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8028
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8028
          initialDelaySeconds: 5
          periodSeconds: 5
      imagePullSecrets:
      - name: registry-credentials
---
apiVersion: v1
kind: Service
metadata:
  name: microinsurance-service
  namespace: insurance-platform
spec:
  selector:
    app: microinsurance-service
  ports:
  - port: 8028
    targetPort: 8028
    name: http
  - port: 9090
    targetPort: 9090
    name: metrics
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: microinsurance-service-hpa
  namespace: insurance-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: microinsurance-service
  minReplicas: 3
  maxReplicas: 15
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: microinsurance-service-pdb
  namespace: insurance-platform
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: microinsurance-service
